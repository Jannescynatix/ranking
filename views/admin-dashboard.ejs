<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="container">
    <header>
        <h1>Admin Dashboard</h1>
        <div class="nav-links">
            <a href="/reviews/dashboard" class="btn-primary">Zurück zum Dashboard</a>
            <a href="/logout" class="btn-danger">Abmelden</a>
        </div>
    </header>

    <div class="admin-section">
        <h2>Alle Produkte und Bewertungen</h2>
        <% if (products.length > 0) { %>
            <% products.forEach(product => { %>
                <div class="product-card">
                    <h3><%= product.name %></h3>
                    <p class="text-muted">Durchschnittliche Bewertungen (<%= product.reviewCount %>):</p>
                    <ul>
                        <li>Qualität: <%= (product.ratings.quality).toFixed(1) %></li>
                        <li>Service: <%= (product.ratings.service).toFixed(1) %></li>
                        <li>Atmosphäre: <%= (product.ratings.atmosphere).toFixed(1) %></li>
                    </ul>

                    <h4>Kommentare:</h4>
                    <% if (product.comments.length > 0) { %>
                        <% product.comments.forEach(comment => { %>
                            <div class="comment-card">
                                <p><%= comment.comment %></p>
                                <form action="/admin/delete-review/<%= comment._id %>" method="POST">
                                    <button type="submit" class="btn-danger">Löschen</button>
                                </form>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p class="text-muted">Noch keine Kommentare.</p>
                    <% } %>
                </div>
            <% }) %>
        <% } else { %>
            <p>Noch keine Produkte bewertet.</p>
        <% } %>
    </div>
</div>
</body>
</html>